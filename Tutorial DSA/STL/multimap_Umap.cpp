 #include <bits/stdc++.h>
 using namespace std;
 int main(){
    multimap<int,int> mp;
    //Multi map has sorted but not unique it can have multiple duplicate keys;

    unordered_map<int,int> um;
    //Unordered map has random but unique it cannot have multiple duplicate keys;

 }