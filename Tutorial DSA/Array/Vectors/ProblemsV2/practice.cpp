#include <bits/stdc++.h>
using namespace std;

/* Given an integer array 'a' sorted in increasing order, return an array of squares of each
     number sorted in increasing order.
     v={-10,8,2,-7,12,15,20}
 */

 sort increasing(vector<int> &v){
    int left_ptr=0;
    int right_ptr=v.size();
    if(abs(v[left_ptr])) 
 }