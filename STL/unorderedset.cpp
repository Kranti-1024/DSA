 #include <bits/stdc++.h>
 using namespace std;
//Unordered sets are not sorted but unique it can have random order
// similar to multiset just unordered

 int main(){
    unordered_set<int> us;

    //Time complexity O(1);
    //Time complexity in wortest case is O(n);
 }